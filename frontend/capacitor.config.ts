import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DHBW-Positioning-System',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
